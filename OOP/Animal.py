class Animal(object):
    def __init__(self,name):
        self.name=name
    
    def walk(seif):
        raise NotImplemented('Subclass must implement the abstract method by self')
    def talk(self):
        raise NotImplemented('Subclass must implement the abstract method by self')
